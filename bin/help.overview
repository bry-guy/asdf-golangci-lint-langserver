#!/usr/bin/env bash
set -eu
[ "${BASH_VERSINFO[0]}" -ge 3 ] && set -o pipefail

cat <<-EOM
Manage golangci-lint-langserver with asdf.

- https://github.com/bry-guy/asdf-golangci-lint-langserver
- https://https://github.com/nametake/golangci-lint-langserver
EOM

